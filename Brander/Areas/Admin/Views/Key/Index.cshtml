@model IEnumerable<Key>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<div class="border container">
    <div class="row">
        <div class="col-6">
            <h2 class="text-white">Lista de Keys</h2>
        </div>
        <div class="col-6 text-right">
            @*Llamando a la partial view*@
            <partial name="_CreateButtonPartial" />
        </div>
    </div>
    <br />

    <div>
        @*El Count() sirve para saber si tabla esta vacio o no*@
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border table-light">
                <thead class="thead-dark">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(m => m.Id)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.Price)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.StockId)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.GameId)
                        </th>

                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(m => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.Price)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.Stock.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.Game.Name)
                        </td>

                        @*Llamamos al partial view que tiene las opciones de edicion,detalles y eliminacion y con el tag model le pasamos la id*@
                        <td>
                            <partial name="_TableButtonpartial" model="item.Id" />
                        </td>
                    </tr>
                }
            </table>
        }
        else
        {
            <div class="text-light">
                <p>
                    No existe ninguna Key
                </p>
            </div>
        }
    </div>
</div>

